"""Central initialization point for future Flask extensions."""

from flask import Flask


def init_extensions(app: Flask) -> None:
    """Initialize Flask extensions registered by the Backoffice."""
    return None
